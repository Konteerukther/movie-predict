# Movie Recommendation System - Deployment Demo

> 🚀 This README focuses **only** on the `Deployment.ipynb` file, which serves as an interactive environment for testing, demonstration, and retraining the recommendation models.

## 📄 File Description

`Deployment.ipynb` (Jupyter Notebook) เป็นไฟล์หลักสำหรับ:
1.  **โหลด (Load)** โมเดลที่เทรนเสร็จแล้ว (SVD-CF และ Content-Based)
2.  **ทดสอบ (Test)** โมเดลแต่ละประเภทแบบแยกส่วน
3.  **รับ Feedback (Input)** จากผู้ใช้ผ่าน UI
4.  **เทรนใหม่ (Retrain)** โมเดล SVD (CF) โดยใช้ Feedback ใหม่

ไฟล์นี้ไม่ได้สร้างโมเดล Content-Based (`.npz`) หรือ TF-IDF ใหม่ แต่จะใช้ไฟล์ที่เทรนไว้แล้วจาก `processed/models/` และ `processed/preprocess/` เท่านั้น

---

## 🏛️ Required File Structure

เพื่อให้ Notebook นี้ทำงานได้ (เนื่องจากใช้ Path แบบ Portable) คุณต้องจัดเรียงไฟล์ตามโครงสร้างนี้ โดยวาง `Deployment.ipynb` ไว้ที่ Root:

```
Your_Project_Folder/
├── 🚀 Deployment.ipynb
│
└── processed/
    ├── cleaned/
    │   ├── movies_cleaned_f.csv
    │   └── ratings_cleaned_f.csv
    │
    ├── models/
    │   ├── content_similarity_sparse.npz
    │   ├── svd_U.npy
    │   ├── svd_Sigma.npy
    │   ├── svd_Vt.npy
    │   ├── svd_user_mean.npy
    │   ├── svd_user_index.pkl
    │   ├── svd_movie_index.pkl
    │   ├── svd_reverse_user_index.pkl
    │   └── svd_reverse_movie_index.pkl
    │
    ├── preprocess/
    │   └── movies_tfidf_reduced.csv
    │
    └── user_feedback.csv  <-- (ไฟล์นี้จะถูกสร้างขึ้นอัตโนมัติ)
```

---

## 💡 Key Features & How to Use

คุณต้องรัน Cell ใน Notebook นี้ตามลำดับ

#### 1. (Cell 1-4) Initialization
* **Cell 1:** Imports (ต้องติดตั้ง `ipywidgets`, `pandas`, `scipy`, `numpy`)
* **Cell 2:** Set Paths (ตั้งค่า Path ไปยังโฟลเดอร์ `processed/`)
* **Cell 3:** Helper Functions (นิยามฟังก์ชันทั้งหมด เช่น `get_content_based_recs`, `recommend_movies`, `run_retrain_pipeline`)
* **Cell 4:** **Load Data & Models** (รันครั้งเดียว) โหลด Artifacts ทั้งหมดเข้าสู่ Memory

#### 2. (Cell 5-8) Model Testing
Cell เหล่านี้ใช้สำหรับทดสอบโมเดลแต่ละส่วน:
* **Cell 5: Test 1 (Content-Based)**
    * **Input:** `MOVIE_TITLE_TO_TEST`
    * **Output:** แนะนำหนังที่ "คล้ายกัน"
* **Cell 6: Test 2.1 (CF/SVD)**
    * **Input:** `USER_ID_TO_TEST`
    * **Output:** แนะนำหนังที่ User น่าจะ "ชอบ"
* **Cell 7: Test 2.2 (CF/SVD)**
    * **Input:** `MOVIE_TITLE_FOR_USERS`
    * **Output:** แนะนำ User ที่น่าจะ "ชอบ" หนังเรื่องนี้
* **Cell 8: Test 3 (Hybrid Model)**
    * **Input:** `USER_ID_HYBRID_TEST`
    * **Output:** แนะนำหนังโดยใช้คะแนนผสม (SVD 70% + Content 30%)

#### 3. (Cell 10) ⭐ Interactive Feedback Widget
UI นี้ใช้ `ipywidgets` เพื่อให้คุณ:
1.  **เปลี่ยน User ID:** สามารถใส่ `userId` ใหม่ (ที่ไม่มีในระบบ) ได้
2.  **ค้นหาหนัง:** พิมพ์ชื่อหนังเพื่อค้นหา
3.  **ให้คะแนน:** เลือก 0.5 - 5.0 ดาว
4.  **บันทึก:** กด "บันทึก Feedback" ข้อมูลจะถูกบันทึก (Append) ลงใน `processed/user_feedback.csv` (และมีการปัดเศษทศนิยมที่เพี้ยน)

#### 4. (Cell 12) 🤖 Retrain Button
ปุ่มนี้จะเรียกใช้กระบวนการเทรน SVD ใหม่ทั้งหมด:
1.  **เช็ก Feedback:** ตรวจสอบ `user_feedback.csv` (ถ้าว่าง จะไม่ทำอะไรต่อ)
2.  **โหลด:** โหลด `ratings_cleaned_f.csv` (ไฟล์เก่า)
3.  **รวม (Combine):** นำไฟล์เก่า + ใหม่ มารวมกัน
4.  **ลบซ้ำ (Deduplicate):** ลบเรตติ้งซ้ำซ้อน (เก็บเฉพาะอันใหม่สุด)
5.  **บันทึกทับ:** บันทึกข้อมูลที่สะอาดแล้วกลับไปที่ `ratings_cleaned_f.csv`
6.  **ล้าง (Clear):** ล้าง `user_feedback.csv` ให้ว่าง
7.  **เทรนใหม่ (Retrain):** รัน `svds(k=50)` ใหม่ทั้งหมด (อาจใช้เวลา 1-5 นาที)
8.  **บันทึกโมเดล:** บันทึกไฟล์ `.npy`, `.pkl` ใหม่ทับของเดิม
9.  **โหลดใหม่ (Reload):** โหลดโมเดลใหม่เข้า Memory อัตโนมัติ

หลังจาก Retrain เสร็จ คุณสามารถกลับไปรัน Cell 5-8 เพื่อดูผลลัพธ์ที่เปลี่ยนไปได้ทันที
